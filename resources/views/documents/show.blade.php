@extends('layouts.public')
@section('title', $document->title)
@section('site-header')<x-public.site-header :site="$site" />@endsection
@section('content')<article class="wm-container py-12 sm:py-16"><div class="mx-auto max-w-4xl"><p class="text-xs font-semibold uppercase tracking-wider text-brand">{{ $document->category->name }}</p><h1 class="wm-heading-xl mt-3">{{ $document->title }}</h1><p class="mt-5 text-ink-muted">{{ $document->description }}</p><a class="mt-8 inline-flex min-h-11 items-center rounded-full bg-brand px-5 font-semibold text-on-brand" href="{{ route('documents.download', [$document->slug, $document->currentVersion]) }}">Download current version</a>@if($document->visibility === 'public' && $document->public_version_history)<section class="mt-12"><h2 class="wm-heading-md">Version history</h2><ul class="mt-4 space-y-2">@foreach($document->versions->sortByDesc('version_number') as $version)<li><a class="text-brand underline" href="{{ route('documents.download', [$document->slug, $version]) }}">Version {{ $version->version_number }}</a></li>@endforeach</ul></section>@endif</div></article>@endsection
@section('site-footer')<x-public.site-footer :site="$site" />@endsection
